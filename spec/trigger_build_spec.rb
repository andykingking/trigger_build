require 'spec_helper'

describe TriggerBuild do
  it 'has a version number' do
    expect(TriggerBuild::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
